#!/bin/bash

site_name=$(basename "$(pwd)")
echo "Starting $site_name on http://localhost:8080..."
docker build -t $site_name .
docker run -it --rm \
  -p 8080:80 \
  -v ./public:/site/public \
  -e GIT_URL=${GIT_URL} \
  --name $site_name \
  $site_name nginx '-g daemon off;'
